---
timestamp: '2026-03-24T05:36:14'
parent: team-lead
child: code-monkey
scope: /Volumes/Proj/proj/RobotProjects/AprilTags
sprint: 007-polish-packaging
ticket: '004'
context_documents:
- docs/clasi/sprints/007-polish-packaging/sprint.md
- docs/clasi/sprints/007-polish-packaging/architecture-update.md
- docs/clasi/sprints/007-polish-packaging/usecases.md
- docs/clasi/sprints/007-polish-packaging/tickets/004.md
result: "success \u2014 5 deprecated CLI modules removed, README added"
files_modified:
- src/aprilcam/cli/__init__.py
- src/aprilcam/cli/aprilcam_cli.py
- src/aprilcam/cli/aprilcap_cli.py
- src/aprilcam/cli/apriltest_cli.py
- src/aprilcam/cli/atscreencap_cli.py
- src/aprilcam/cli/homocal_cli.py
- tests/test_cli_smoke.py
- README.md
---

# Dispatch: team-lead → code-monkey

Implement tickets #004 and #005 for sprint 007 together. Scope: /Volumes/Proj/proj/RobotProjects/AprilTags

## Ticket 004: Remove deprecated CLI modules

Read the project structure first. Look for CLI modules that are no longer needed now that the MCP server handles everything. Check:
- `src/aprilcam/cli/` directory for deprecated modules
- `pyproject.toml` for any remaining legacy entry points
- Imports of deprecated modules

Keep: `aprilcam.cli` main module (the `aprilcam` CLI entry point), `taggen`, `arucogen`, `cameras` subcommands.
Remove: any standalone CLI modules for features now handled by MCP (like `atscreencap_cli`, `apriltest_cli`, `playfield_cli`, `homocal` if they exist).

Be careful — read each file before removing to confirm it's truly deprecated and not imported elsewhere. Use grep to check for imports.

Clean up any unused imports in remaining files.

## Ticket 005: README

Create `README.md` at the project root with:

1. **Project title and description** — AprilCam MCP Server: An MCP server for AI agents to interact with cameras, fiducial markers, and playfields.

2. **Installation** — `pipx install aprilcam` or `pip install aprilcam`

3. **MCP Server Usage** — How to start: `aprilcam mcp`. How to configure in Claude Code (`.mcp.json` example).

4. **MCP Tools Reference** — Brief list of all available tools organized by category:
   - Camera Management: list_cameras, open_camera, close_camera, capture_frame
   - Playfield: create_playfield, create_playfield_from_image, calibrate_playfield, get_playfield_info, deskew_image
   - Tag Detection: start_detection, stop_detection, get_tags, get_tag_history
   - Image Processing: get_frame, detect_lines, detect_circles, detect_contours, detect_motion, detect_qr_codes, crop_region, apply_transform
   - Multi-Camera: create_composite, get_composite_frame, get_composite_tags

5. **CLI Subcommands** — `aprilcam mcp`, `aprilcam taggen`, `aprilcam arucogen`, `aprilcam cameras`

6. **Requirements** — Python >=3.9, OpenCV (contrib), camera hardware for live use

Keep it concise. No emojis.

## Git
- Commit 1: `chore: remove deprecated CLI modules (#004, sprint 007)`
- Commit 2: `docs: add README with installation and usage instructions (#005, sprint 007)`
- `uv run pytest` must pass after each
- Do NOT move tickets to done

## Context Documents

- `docs/clasi/sprints/007-polish-packaging/sprint.md`
- `docs/clasi/sprints/007-polish-packaging/architecture-update.md`
- `docs/clasi/sprints/007-polish-packaging/usecases.md`
- `docs/clasi/sprints/007-polish-packaging/tickets/004.md`
