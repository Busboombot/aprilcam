---
timestamp: '2026-03-24T02:52:16'
parent: team-lead
child: code-monkey
scope: /Volumes/Proj/proj/RobotProjects/AprilTags
sprint: 004-tag-detection-loop-ring-buffer
ticket: '007'
context_documents:
- docs/clasi/sprints/004-tag-detection-loop-ring-buffer/sprint.md
- docs/clasi/sprints/004-tag-detection-loop-ring-buffer/architecture-update.md
- docs/clasi/sprints/004-tag-detection-loop-ring-buffer/usecases.md
- docs/clasi/sprints/004-tag-detection-loop-ring-buffer/tickets/007.md
result: "success \u2014 5 integration tests added, 118 total passing"
files_modified:
- tests/test_mcp_detection.py
---

# Dispatch: team-lead → code-monkey

Implement ticket #007 for sprint 004. Scope: /Volumes/Proj/proj/RobotProjects/AprilTags

## Ticket: Integration tests for detection MCP tools

Add integration tests to the existing `tests/test_mcp_detection.py` file. The file already has basic tests from tickets 005/006. Add more comprehensive integration tests.

### New tests to add

1. `test_full_detection_roundtrip` — open mock camera, start_detection, sleep(1), get_tags, get_tag_history, stop_detection. Verify each step succeeds.

2. `test_tag_positions_match_expected` — start detection with playfield_cam3_moved.jpg, sleep(0.5), get_tags, verify at least some tags are detected (check "tags" list is non-empty and each tag has "id" and "center_px").

3. `test_velocity_near_zero_static` — start detection with a single static image (same image every frame), sleep(0.5), get_tags, verify that for tags with velocity data, speed_px is small (< 1.0 or None).

4. `test_all_json_fields_present` — start detection, sleep(0.5), get_tags, for each tag in response verify ALL expected keys exist: id, center_px, corners_px, orientation_yaw, world_xy, in_playfield, vel_px, speed_px, vel_world, speed_world, heading_rad, timestamp, frame_index.

5. `test_history_chronological_order` — start detection, sleep(0.5), get_tag_history(num_frames=10), verify frames are in ascending timestamp and frame_index order.

6. `test_error_get_tags_no_loop` — (may already exist, skip if so)

7. `test_error_stop_unknown_source` — (may already exist, skip if so)

8. `test_error_start_duplicate` — (may already exist, skip if so)

### Important notes
- Read the existing test file first to see what's already there and avoid duplicates
- Use the existing FakeCapture class and clean_registries fixture
- All tests use `@pytest.mark.asyncio` or `_run()` helper
- Import `start_detection, stop_detection, get_tags, get_tag_history` from `aprilcam.mcp_server`
- Test image: `tests/data/playfield_cam3_moved.jpg`
- Always stop detection loops in try/finally to avoid dangling threads

### Git
- Run `uv run pytest` — all must pass
- Commit: `test: add integration tests for detection MCP tools (#007, sprint 004)`
- Do NOT move ticket to done

## Context Documents

- `docs/clasi/sprints/004-tag-detection-loop-ring-buffer/sprint.md`
- `docs/clasi/sprints/004-tag-detection-loop-ring-buffer/architecture-update.md`
- `docs/clasi/sprints/004-tag-detection-loop-ring-buffer/usecases.md`
- `docs/clasi/sprints/004-tag-detection-loop-ring-buffer/tickets/007.md`
